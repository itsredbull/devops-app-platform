apiVersion: v1
kind: Service
metadata:
  name: uptime-api
  labels:
    app.kubernetes.io/name: uptime-api
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: uptime-api
  ports:
    - name: http
      port: 80
      targetPort: 8080
